# clinic.fe

Minimal Angular + PrimeNG frontend for clinic.ai.

Getting started:

1. From `FE/` run:

```powershell
npm install
npm run start
```

2. App will run at `http://localhost:4200` and proxy `/fhir` to `http://localhost:8082`.
